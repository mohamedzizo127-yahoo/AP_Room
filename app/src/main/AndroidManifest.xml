<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aproom">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.EditCardActivity"></activity>
        <activity android:name=".ui.EditSubjectActivity" />
        <activity
            android:name=".ui.PracticeListActivity"
            android:label="@string/title_activity_practice_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.CardListActivity"
            android:label="@string/title_activity_card_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.SubjectListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>